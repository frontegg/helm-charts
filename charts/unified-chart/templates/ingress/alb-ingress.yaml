{{- if .Values.albIngress.enabled -}}
{{- $name := include "fuc.name" . -}}
{{- $svcPort := .Values.service.port -}}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ required ".Values.serviceName is required" .Values.serviceName }}-alb-ingress
  labels:
    {{- include "fuc.labels" . | nindent 4 }}
  {{- with .Values.albIngress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.albIngress.ingressClassName }}
  ingressClassName: {{ . | quote }}
  {{- end }}
  rules:
    - host: {{ include "fuc.hostname" . | quote }}
      http:
        paths:
        - path: /
          backend:
            service:
              name: frontegg-{{ include "fuc.name" . }}
              port:
                number: {{ $svcPort }}
          pathType: ImplementationSpecific
{{- end }}