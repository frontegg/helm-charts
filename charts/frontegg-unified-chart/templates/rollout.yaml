{{- if .Values.argoRollouts }}
---
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: {{ include "tenants-service.fullname" . }}-rollout
spec:
  workloadRef: # Reference an existing Deployment using workloadRef field
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "tenants-service.fullname" . }}-deployment
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      {{- include "tenants-service.selectorLabels" . | nindent 6 }}
  strategy:
    blueGreen:
      activeService: frontegg-{{ include "tenants-service.name" . }}
      autoPromotionEnabled: true
{{- end }}