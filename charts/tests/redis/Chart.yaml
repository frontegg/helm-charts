apiVersion: v2
name: redis
description: A Helm chart for Kubernetes
type: application
version: 0.1.0

dependencies:
  - name: redis
    # version: "17.1.*"
    version: "18.19.*"
    repository: "https://charts.bitnami.com/bitnami"
